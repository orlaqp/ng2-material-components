include ../../../mixins/mixins

section#content
    .container
        .block-header
            h2 User Interface Components

        each comp in components
            .card
                .card-header
                    p.lead #{comp.title}
                .card-body.card-padding
                    .readme!= comp.readme
                    .demo
                         #{'demo-' + comp.key}
                    +code-preview(comp)
