import { EmailPipeConfig } from './email-pipe';
declare var _default: {
    mask: (rawValue: string, config: EmailPipeConfig) => any;
    pipe: (conformedValue: string, config: EmailPipeConfig) => any;
};
export default _default;
