button.btn.waves-effect(class="bgm-{{color}} c-{{simpleColor}} btn-{{size}}", type="{{type}}",
    [class.btn-icon-text]="iconAndtext",
    [class.btn-block]="block",
    [class.waves-circle]="circular || rounded",
    [class.waves-float]="circular || rounded",
    [class.btn-icon]="circular",
    [class.btn-rounded]="rounded",
    [class.btn-link]="simple",
    [attr.disabled]="disabled",
    (click)="onClick($event)")
    i.zmdi(*ngIf="icon", class="zmdi-{{icon}}")
    | {{title}}
