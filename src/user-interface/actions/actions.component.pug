ul.actions(
    class="c-{{color}}",
    [ngClass]="{ 'actions-alt': alt, 'dropdown': dropdown }",
    [class.actions]="!showBig",
    [class.top-menu]="showBig")
    li(*ngFor="let item of actionItems", [actionItem]="item")
