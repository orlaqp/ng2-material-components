.toggle-switch([class.disabled]="disabled", [attr.data-ts-color]="color")
    label.ts-label([attr.for]='identifier') {{label}}
    input(
        [attr.id]="identifier",
        type='checkbox',
        hidden='hidden',
        [disabled]="disabled",
        [formControl]="control")
    label.ts-helper([attr.for]='identifier')
