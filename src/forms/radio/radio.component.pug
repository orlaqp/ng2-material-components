.radio.m-b-15([class.disabled]="disabled")
  label
    input(type='radio', name="{{groupName}}",
        [attr.disabled]="disabled",
        [attr.value]="value",
        (click)="check()")
    i.input-helper
    | {{label}}
