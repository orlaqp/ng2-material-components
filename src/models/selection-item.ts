export class SelectionItem {

    constructor(
        public id?: number | string,
        public title?: string,
        public selected?: boolean,
        public disabled?: boolean) {

        }
}
